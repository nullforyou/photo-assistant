# 2026-08-20 工作日志

## photo-assistant Flutter 项目（更正记录）
- **关键更正**：用户本机已有 Flutter，位于 `D:\Program Files\flutter\flutter_windows_3.47.1-stable`（3.47.1 / Dart 3.13.1）。此前误以为没装、克隆了一份到 `D:\d\src\flutter`，但该克隆并未持久化（现为空白目录），**用户真实 Flutter 一直完好、未受任何影响**。
- 项目 `D:\work\拍照助手\photo-assistant` 已存在且完整（flutter create 生成，含全平台目录；欢迎页在 `lib/main.dart`）。
- 验证：用用户的真实 Flutter 跑 `flutter --version` 正常（3.47.1/Dart 3.13.1/DevTools 2.60.0）；`dart analyze` 通过（No issues found!）。

## 真正踩到的坑
- `flutter analyze` 在本环境（及可能因中文路径）崩溃：LSP 分析服务器解析 JSON 时卡在中文路径 `拍照助手`（`%E6%89%8B`），exit 255。代码无错，改用 `dart analyze` 即可验证。建议迁到英文路径。

## 待办
- Android SDK 尚未安装，需在 Android Studio 装好 SDK + 模拟器，才能真正 `flutter run` 在设备上看到欢迎页。
- IDEA 装好后，Flutter SDK 路径指向 `D:\Program Files\flutter\flutter_windows_3.47.1-stable`。
- 清理误建的空白目录 `D:\d\src\flutter`（及根目录临时文件 `src_check.txt` 等）。

## 路径中文化解（22:22）
- 用户要求把中文路径改成英文。已将项目从 `D:\work\拍照助手\photo-assistant` 迁移到 `D:\work\photo-assistant`（纯英文）。
- 迁移后重跑 `flutter analyze`：**No issues found! (ran in 5.4s)**，此前 LSP 崩溃消失，证实根因确为父目录中文 `拍照助手`。
- 结论：Flutter 项目务必放在纯英文路径下。

## Flutter 演示运行（22:45）
- `flutter run -d windows` 失败：缺少 Visual Studio C++ 工具链（编译 Windows 桌面版必需）。
- 改用 `flutter run -d chrome` 成功，应用已启动（欢迎页正常显示）。
- Flutter 不在用户 PATH 中，需用完整路径 `/d/Program Files/flutter/flutter_windows_3.47.1-stable/bin/flutter`。
- 结论：演示用 Chrome 版；若要跑 Windows 原生版需装 Visual Studio（含 C++ 桌面开发工作负载）。

## PATH 与 VS 安装（23:00-23:45）
- 已完成：Flutter bin 目录写入用户 PATH（注册表），新开的终端直接敲 `flutter` 可用。
- 已完成：winget 静默安装 VS 2022 Community 17.14.39（含 C++ 桌面开发工作负载），`flutter doctor` 识别正常。
- 新问题：`flutter build/run -d windows` 构建阶段必崩，CMake 报 MSBuild "Access violation"。排查发现：
  - CMake 配置阶段正常（编译器检测通过、sln/vcxproj 全部生成），仅构建阶段崩。
  - Bash/PowerShell 双通道、单进程模式（/m:1 /nodeReuse:false）均失败；devenv.com 能过 ZERO_CHECK 但停在 flutter_assemble（「已取消一个任务」）。
  - Windows 事件日志无崩溃记录 → 驱动级拦截特征。
  - 机器同时跑 Windows Defender 实时防护 + 联想电脑管家火绒内核（usysdiag.exe）→ 双杀软嫌疑最大。
- 待办：用户在自己终端跑 `flutter run -d windows` 做决定性测试；若同样失败，需在 Defender 和联想电脑管家中加白名单（排除 VS/项目/Flutter 目录）。
